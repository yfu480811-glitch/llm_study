diff --git a/README.md b/README.md
index b6ae79b5029af9778ff47d7ea1684f0dff98a4ef..db0d020fd98de565ae96c88a00c627b51d925534 100644
--- a/README.md
+++ b/README.md
@@ -1,46 +1,158 @@
-## Qwen2.5-1.5B-Instruct 微调示例（GSM8K）
+# Local RAG 知识库问答 Demo（Ollama + FAISS + Streamlit）
 
-这个仓库包含一个可直接运行的脚本，用 `transformers + datasets` 对
-`Qwen/Qwen2.5-1.5B-Instruct` 做监督微调（SFT）。默认数据集是 `gsm8k`，
-你也可以替换为你能获取到的任何 Hugging Face 数据集。
+一个可直接运行的本地 RAG 项目：支持上传文档（txt/md/pdf）→ 切分 → Embedding → FAISS 检索 → 基于检索结果生成回答。
 
-### 1) 安装依赖
+> 设计目标：轻量、低内存、可本机运行，适合写进简历展示“手写 RAG pipeline”。
+
+---
+
+## 1. 功能概览
+
+- 文档导入：支持 `.txt` / `.md` / `.pdf`（PDF 使用 `pypdf`，失败会提示）。
+- Chunking：`chunk_size`、`chunk_overlap` 可调（默认 500 / 100）。
+- Embedding：通过 Ollama 本地 API 调用 `nomic-embed-text`。
+- 向量库：使用 FAISS（`data/index.faiss`）+ 元数据（`data/docs.json`）持久化。
+- 增量更新：按文件 hash 去重，重复上传不会重复入库。
+- 检索：`top_k` 可配置，返回片段与来源（文件名 + 页码）。
+- 回答：`deepseek-r1:1.5b`，系统提示词强约束“仅基于检索内容回答”。
+- Web UI：Streamlit 页面包含上传、建库、参数调节、聊天问答、引用片段展开。
+
+---
+
+## 2. 项目结构
+
+```text
+.
+├── app.py
+├── rag
+│   ├── __init__.py
+│   ├── ingest.py
+│   ├── ollama_client.py
+│   ├── pipeline.py
+│   ├── prompt.py
+│   ├── retriever.py
+│   └── store.py
+├── data
+│   └── (运行后自动生成 index.faiss / docs.json)
+├── requirements.txt
+└── README.md
+```
+
+---
+
+## 3. 环境准备
+
+- Python 3.10+
+- Windows/macOS/Linux
+- 本地可访问 Ollama：`http://localhost:11434`
+
+### 3.1 安装 Ollama 与模型
+
+先确认 Ollama 服务已启动（不同系统启动方式略有差异）。然后拉取模型：
+
+```bash
+ollama pull deepseek-r1:1.5b
+ollama pull nomic-embed-text
+```
+
+可用以下命令检查：
+
+```bash
+ollama list
+```
+
+---
+
+## 4. 安装依赖
 
 ```bash
 pip install -r requirements.txt
 ```
 
-### 2) 运行微调（GSM8K 默认配置）
+> 若你不需要 PDF，可移除 `pypdf`；若使用 PDF 建议保留。
+
+---
+
+## 5. 启动应用
 
 ```bash
-python train_qwen25_gsm8k.py \
-  --output_dir ./qwen25-gsm8k-sft \
-  --num_train_epochs 2 \
-  --per_device_train_batch_size 2 \
-  --gradient_accumulation_steps 8 \
-  --bf16
+streamlit run app.py
 ```
 
-> 如果你的 GPU 不支持 bf16，请改成 `--fp16` 或不加混精参数。
+启动后在浏览器中：
+
+1. 上传文档（txt/md/pdf）
+2. 点击“建立 / 更新索引”
+3. 输入问题开始问答
+4. 在回答下方展开“查看引用片段”查看证据来源
+
+---
+
+## 6. 参数建议（默认值说明）
+
+- `chunk_size=500`
+  - 小模型 + 本地 CPU 场景下，500 字符通常能保持足够语义且不至于太重。
+- `chunk_overlap=100`
+  - 减少跨段落切分导致的信息断裂，提高召回连续性。
+- `top_k=4`
+  - 保持上下文足够且不过长，兼顾速度与准确性。
+- `temperature=0.2`
+  - 降低发散，提升“依据证据回答”的稳定性。
+
+---
+
+## 7. 常见报错排查
+
+### 7.1 无法连接 Ollama / 端口错误
+
+- 检查服务是否启动。
+- 检查 URL 是否正确：`http://localhost:11434`
+- 检查端口占用：
+
+```bash
+# macOS/Linux
+lsof -i :11434
+
+# Windows PowerShell
+netstat -ano | findstr 11434
+```
 
-### 3) 可替换为其他数据集
+### 7.2 模型不存在
 
-脚本支持通过参数替换数据集和字段名，例如：
+当页面提示模型调用失败时，请确认模型名拼写并重新拉取：
 
 ```bash
-python train_qwen25_gsm8k.py \
-  --dataset_name your_dataset_name \
-  --dataset_config your_config \
-  --train_split train \
-  --eval_split validation \
-  --question_field instruction \
-  --answer_field output
+ollama pull deepseek-r1:1.5b
+ollama pull nomic-embed-text
 ```
 
-只要数据里有“问题字段 + 答案字段”，就可以复用这套流程。
+### 7.3 Embedding 接口兼容问题
+
+代码中优先调用 `/api/embeddings`，若接口不存在会自动回退 `/api/embed`。
+
+### 7.4 FAISS 安装失败
+
+- 建议先升级 pip：`python -m pip install -U pip`
+- 再安装：`pip install faiss-cpu`
+
+### 7.5 PDF 解析失败
+
+- 扫描版 PDF 可能提取不到文本（这是 OCR 场景）
+- 可先转为可复制文本，或优先用 txt/md 导入
+
+---
+
+## 8. 简历描述要点（可直接参考）
+
+- 独立设计并实现本地 RAG Pipeline（文档解析、切分、向量化、检索、生成）
+- 基于 Ollama 本地 API 封装 Chat 与 Embedding 双通道调用，支持接口兼容回退
+- 使用 FAISS 构建可持久化向量索引，实现文件哈希去重与增量入库
+- 通过系统提示词与证据拼接策略约束模型“仅基于检索内容回答”
+- 使用 Streamlit 搭建可交互知识库问答 UI，支持引用片段可视化与参数调优
+
+---
 
-### 4) 说明
+## 9. 备注
 
-- 脚本会把样本转换为聊天格式（system / user / assistant），并调用 Qwen tokenizer 的 chat template。
-- 训练后模型和 tokenizer 会保存到 `--output_dir`。
-- 默认使用 full fine-tuning（不是 LoRA），显存压力较大；若你需要，我可以继续给你补一版 LoRA/QLoRA 脚本。
+- 首次建库速度取决于文档大小与 CPU 性能。
+- 无独显场景下建议控制文档规模，并优先使用较小 top_k 与较短上下文。
